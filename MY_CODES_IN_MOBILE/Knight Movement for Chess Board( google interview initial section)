#include<stdio.h>
void function (int i,int j,int n,int grid[10][10]){
//use numerical value of n inside grid [][]


if(i>=n||j>=n||i<0||j<0){return ;}
if(i==-1||i==-2||j==-1||j==-2||grid[i][j]==2){return;}
if (grid[i][j]!=2||grid[i][j]==0){
printf("\nknight moved to:(%d ,%d),",i,j);

if(i==n-1&&j==n-1){
printf("\n reached the end row and end column : (%d, %d)\n",i,j);

return;
}

grid[i][j]=2;
function (i+2,j+1,n,grid);

function (i+2,j-1,n,grid);

function (i-2,j+1,n,grid);

function (i-2,j-1,n,grid);

function (i+1,j+2,n,grid);

function (i+1,j-2,n,grid);

function (i-1,j+2,n,grid);

function (i-1,j-2,n,grid);


}

}

int main (){int i; int j;
int n=8;// write numerical value of n
    int grid[8][8] = {{0}};
   
//enter elements only 1 or 0;
// use numerical value of n in grid[][]

printf("enter starting point in maize\n");
scanf("%d %d",&i,&j);
printf(" new island traversal start from :(%d, %d)",i,j);
//if(grid[i][j]==0){printf("\n no such path exist");}
function (i,j,n,grid);

printf("\n 1)if reached to end column and end row can not be seen,then  statement no 3is false");
printf("\n 2) if reched to end column and end row can seen ,then  statement no 3 is true");
if(i!=n-1 || j!=n-1){
   printf("\n statement(3) such path not exist ");
}

printf("\n");

return 0;

}

