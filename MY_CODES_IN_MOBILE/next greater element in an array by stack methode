#include<stdio.h>
#include<stdlib.h>
#define element_no 9

//int ans[element_no]={-1,-1,-1,-1,-1,-1,-1,-1,-1};
struct stack{

int size;
int top;
int*arr;

};
int isstackfull(struct stack *s){
if(s->top>=s->size-1){return 1;
    }
  else{return 0;}
}

int isstackempty (struct stack *s){
if(s->top==-1){return 1;//it is true that stack is empty
}
  else{return 0;}//it is false that stack is empty
  }

void push(struct stack *s,int data){
if(isstackfull(s)==1){
printf("stack is overflowed/ stack is full");
return ;}

else{
s->top++;
s->arr[s->top]=data;
return ;
   }
}

void pop(struct stack *s){
if(isstackempty(s)==1){
printf("stack is underflow/stack is empty");
}

else{
   s->top--;
  
   }
}
showstacktop(struct stack*s){
  printf("%d\n",s->arr[s->top]);
}
showstackbottom (struct stack*s){
  printf("%d\n",s->arr[0]);
}
printfuction(struct stack *s){
if(isstackfull(s)==1){
printf("stack is overflowed/ stack is full");}
else{ for(int i=0;i<=s->top;i++){

printf("%d ",s->arr[i]);
  }}}





int main(){

struct stack*s;
s->size=80;
s->top=-1;
s->arr=(int*)malloc(s->size*sizeof(int));

int ARR[element_no]={8,6,4,7,4,9,10,8,12};

int ans[element_no]={-1,-1,-1,-1,-1,-1,-1,-1,-1};

push(s,0);//starting index of array
for(int i=1;i<element_no;i++){
//printf("%d",ans[i]);

if(ARR[s->arr[s->top]]>ARR[i]){

push(s,i);}

if(ARR[i]>ARR[s->arr[s->top]]){
int temp=s->top+1;
while (!isstackempty(s) && ARR[s->arr[s->top]]<ARR[i]){
//printfuction(s);
//int temp=s->top+1;

ans[s->arr[s->top]]=ARR[i];
//printf("%d ",ans[i]);
pop(s);
   }
  
   push(s,i);
}}

for(int i=0;i<element_no;i++){

printf("%d ",ARR[i]);}

printf("\n");


for(int i=0;i<element_no;i++){

printf("%d ",ans[i]);}

//plexity 0(1)

return 0;


}

